name: Frontend CI

on:
  push:
    branches: ['main']
    paths:
      - 'apps/frontend/**'
      - 'package.json'
      - 'package-lock.json'
  pull_request:
    branches: ['main']
    paths:
      - 'apps/frontend/**'
      - 'package.json'
      - 'package-lock.json'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Frontend
        run: npm run build:frontend

      - name: Test Frontend
        run: npm run test:frontend

      - name: Lint Frontend
        run: npx eslint apps/frontend
